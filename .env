# Flask Environment Variables
# In development, you can set this to 'development' for debug mode.
FLASK_ENV=development
FLASK_PORT=5000
ENV=development

# A secret key is required for session management and other security features.
# Generate a secure, random key for your production application.
# You can generate one with: python -c 'import secrets; print(secrets.token_hex())'
SECRET_KEY=replace-this-with-a-real-secret-key

# Question Bank Source Directory
QB_SOURCE_DIR=data
QB_MANIFEST_FILE_NAME=topics_manifest.json
